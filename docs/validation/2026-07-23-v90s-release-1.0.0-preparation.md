# V90S Release 1.0.0 Preparation

Date: 2026-07-23
Image source commit: `901ad50394a98e6958a9848d0a19b52dcd317634`
Host result: PASS
Real-device result: PASS (user-confirmed)
Publication result: pending

## Release Identity

```text
repository=game-de-it/plumOS-V90S_V2
planned_tag=plumOS-V90S_V2-v1.0.0
release_title=plumOS V90S v1.0.0
vendor_runtime=v90s-stockos-r1
```

The target GitHub repository name was available when checked. This preparation
step does not create the repository, push source, create the tag, or publish a
GitHub Release.

## Release Bundle

The ignored local release directory is:

```text
dist/plumos-v90s-release-v1.0.0/
```

It contains:

- `plumos-v90s-v1.0.0-sd-image.7z`
- `plumos-v90s-v1.0.0-sd-image.7z.sha256`
- `plumos-v90s-v1.0.0-image-manifest.txt`
- `plumos-v90s-v1.0.0-image-verify.txt`
- `RELEASE_NOTES.md`
- `manifest.txt`
- `SHA256SUMS`

`RELEASE_NOTES.md` follows the A30/MMF release-page format with a complete
English section followed by a complete Japanese section. It includes the asset
description, installation procedure, safety notes, checksums, and clean-clone
build command for the new repository name.

## Artifact Identity

```text
image_name=plumos-v90s-release-1.0.0-vendor-r1.img
image_size=2840088576
image_sha256=ded3c2db2439bab6e2595ba12d218ec160b8e046c64a327ec0fe23356c854ea9
archive_name=plumos-v90s-v1.0.0-sd-image.7z
archive_size=395103335
archive_sha256=2b98d0d22b49280f722918b75884445f449ee19fe41519915bb8e7103cee5a97
```

The archive stores the `.img` at its root. `7zz t` passed, and streaming the
archived image through SHA-256 reproduced the original image hash exactly.
Every entry in `SHA256SUMS` passed independent verification.

## Release Gates

The final checks passed:

- `git fsck --full`
- `git diff --check`
- 32 Python unit tests
- shell syntax checks and Python bytecode compilation
- strict app-layer `checksums.sha256`
- app-layer license audit
- four-partition preflight
- final image verifier
- no ROM file in the release app-layer
- empty PPSSPP factory `MacAddress`
- no tracked private-key, GitHub-token, or AWS-key marker
- no tracked private credential path in reachable history
- no tracked blob at or above 100,000,000 bytes

The largest tracked blob is the versioned System baseline at 96,395,720 bytes,
which is below GitHub's per-file limit.

## Physical Result

The user flashed the final image and confirmed that it boots and passes the
requested operational checks on the POWKIDDY V90S. The StockOS-compatible
six-step backlight route is recorded separately in
`2026-07-23-v90s-stockos-backlight.md`.

This is a user-reported PASS; no new device command transcript was captured for
the final confirmation.
