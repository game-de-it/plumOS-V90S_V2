# V90S Release 1.0.0 SD Image

Date: 2026-07-23
Source implementation commit: `eadf55c`
Host result: PASS
Real-device result: PASS (user-confirmed)

## Purpose

Generate the four-partition provisioning SD image for plumOS V90S 1.0.0
against the fixed `v90s-stockos-r1` vendor runtime.

The supported `release-image` target was run end to end in incremental mode.
It checks and materializes the tracked non-emulator baseline, checks and
prepares the tracked vendor baseline, checksum-verifies the reusable emulator
outputs, then always assembles the strict app-layer, boot package, boot image,
and verified SD image.

The tracked PortMaster baseline was refreshed to adapter version 11 after the
physical Donut Dodo control validation. Its archive contains
`plumos-portmaster-frt`; its SHA-256 is
`77df6ecc25eb2924eea5df246916642d8b2e17f445df6ac20176d5992d8cc340`.

The frontend and System baselines also include the StockOS six-step
`sunxi_backlight` route documented in
`2026-07-23-v90s-stockos-backlight.md`.

## Version and Runtime Checks

```text
system_version=1.0.0
app_layer_version=1.0.0
app_layer_complete=true
compat_vendor=v90s-stockos-r1
mount_path=/mnt/plumos
libretro_core_count=118
minimum_libretro_core_count=118
ppsspp_factory_MacAddress=
```

The app-layer build also passed the license audit:

```text
libretro_core_license_files=114
standalone_license_files=10
portmaster_upstream_license_files=26
```

## Build

```text
./scripts/docker-build.sh release-image --version 1.0.0 --incremental

cache=hit component=retroarch
cache=hit component=cores
cache=hit component=picoarch
cache=hit component=standalone
```

## Artifact

```text
path=output/images/plumos-v90s-release-1.0.0-vendor-r1.img
size=2840088576
sha256=ded3c2db2439bab6e2595ba12d218ec160b8e046c64a327ec0fe23356c854ea9
layout=plumos-v90s-four-partition-seed-v1
minimum_sd_gb=16
```

Input identities:

```text
boot0_sha256=638d5d75f9d348a4fbcadb901a2c102fd88148900c3d186acc7ae6d4095784c4
boot_package_sha256=f991e430bd7d290b8f3b7f2b9dd8071c5989867d7f3fbea0b95ba99939585ec9
boot_image_sha256=ac448534b5c7bd43b4c9d59401af86314c182859d40bb4c8b639fbeae2d688ca
system_squashfs_sha256=cb7651fe18c43e9134115c2afd9301f93e9375e0f82724232932bbceed8e783b
app_runtime_manifest_sha256=80b6a5a553f22b2d97f4bca0a74317bf092a94bd7b25504c4d94d56e7eb15e1b
```

## Host Verification

The strict app-layer build, license audit, and full image preflight passed. The
generated `.sha256` file also passed an independent `shasum -a 256 -c` check.
All 32 automated tests passed. The strict app-layer checksum verification also
passed with `adapter_version=11` and executable
`bin/plumos-portmaster-frt` present.
The System SquashFS contains `sunxi-backlight.ko` with SHA-256
`ac6c0ece870c45fa8bd08de83a03a83263e779d4d4431ed4ab215a64b6db555c`
and the generated init loads it before app-layer startup.
The image verifier passed:

- 640x480 24-bit V90S boot logo source
- GPT seed geometry: p1 1024 MiB, p2 64 MiB, p3 1600 MiB, p4 absent
- raw boot0 and boot-package regions
- p2 provisioning Android boot image
- p1 PLUMBOOT resources and A/B System SquashFS payloads
- p3 PLUMOS_SYS ext4 runtime and frontend payload
- 347 MiB free in the p3 seed

First boot remains responsible for expanding p3 to 8192 MiB and creating p4
FAT32 `PLUMOS` through the remaining card capacity.

## Real-Device Result

The user flashed the exact release image and confirmed on 2026-07-23 that it
boots and that the requested operational checks pass on the V90S. This closes
the physical boot gate for the 1.0.0 image and includes the StockOS backlight
route documented in `2026-07-23-v90s-stockos-backlight.md`.

This final confirmation is recorded as a user-reported PASS. The host-side
hash, image verifier output, and detailed component evidence above remain the
reproducible artifact proof; no new device command transcript was captured for
the final confirmation.
