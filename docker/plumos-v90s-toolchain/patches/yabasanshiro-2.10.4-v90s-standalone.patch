diff --git a/yabause/src/CMakeLists.txt b/yabause/src/CMakeLists.txt
index c8af60029..2f02cd4a2 100644
--- a/yabause/src/CMakeLists.txt
+++ b/yabause/src/CMakeLists.txt
@@ -330,12 +330,14 @@ if (YAB_WANT_OPENGL AND (YAB_RGB STREQUAL ""))
 		)
 		add_definitions(-DYAB_PORT_OSD)
 		
-		include(FindGLUT)
-		if (GLUT_FOUND)
-			message (WARNING "Deprecated Glut found. Support will be removed in the future. ")
-			include_directories(${GLUT_INCLUDE_DIR})
-			add_definitions(-DHAVE_LIBGLUT=1)
-			set(YABAUSE_LIBRARIES ${YABAUSE_LIBRARIES} ${GLUT_LIBRARIES})
+		if (NOT USE_EGL)
+			include(FindGLUT)
+			if (GLUT_FOUND)
+				message (WARNING "Deprecated Glut found. Support will be removed in the future. ")
+				include_directories(${GLUT_INCLUDE_DIR})
+				add_definitions(-DHAVE_LIBGLUT=1)
+				set(YABAUSE_LIBRARIES ${YABAUSE_LIBRARIES} ${GLUT_LIBRARIES})
+			endif()
 		endif()
 
 		# glXGetProcAddress
@@ -436,7 +438,7 @@ if (NOT MINI18N_INCLUDE_DIR STREQUAL "MINI18N_INCLUDE_DIR-NOTFOUND" AND NOT MINI
 endif (NOT MINI18N_INCLUDE_DIR STREQUAL "MINI18N_INCLUDE_DIR-NOTFOUND" AND NOT MINI18N_LIBRARY STREQUAL "MINI18N_LIBRARY-NOTFOUND")
 
 # xrandr
-if (NOT APPLE)
+if (NOT APPLE AND NOT USE_EGL)
 	find_library(XRANDR_LIBRARY Xrandr)
 	if(XRANDR_LIBRARY)
 		add_definitions(-DHAVE_LIBXRANDR=1)
@@ -846,8 +848,6 @@ set(YAB_OPTIMIZATION "" CACHE STRING "Override optimization level")
 
 if( CMAKE_CXX_COMPILER_ID MATCHES "Clang" )
 	set(CMAKE_TOOLCHAIN_PREFIX "llvm-")
-	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -stdlib=libc++")
-	set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -stdlib=libc++ -lc++")
 endif()
 
 if("${CMAKE_SYSTEM_PROCESSOR}" STREQUAL "i686")
@@ -875,8 +875,8 @@ if("${CMAKE_SYSTEM_PROCESSOR}" STREQUAL "armv7-a")
 endif()
 if("${CMAKE_SYSTEM_PROCESSOR}" STREQUAL "aarch64")
 		add_definitions(-DAARCH64)
-    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -g -std=c99 -march=armv8-a -mtune=cortex-a57.cortex-a53 ")
-    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -march=armv8-a -mtune=cortex-a57.cortex-a53 -std=c++11 ")
+    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -g -std=c99 -march=armv8-a -mtune=cortex-a53 ")
+    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -march=armv8-a -mtune=cortex-a53 -std=c++11 ")
 endif()
 
 # Warnings defined to know when we're breaking compilation with MSVC
diff --git a/yabause/src/retro_arena/CMakeLists.txt b/yabause/src/retro_arena/CMakeLists.txt
index 8a32c79f4..2cd346183 100644
--- a/yabause/src/retro_arena/CMakeLists.txt
+++ b/yabause/src/retro_arena/CMakeLists.txt
@@ -9,6 +9,7 @@ find_package(SDL2 COMPONENTS sdl)
 find_package(SDL2 REQUIRED)
 find_package(OpenGLES)
 find_package(PkgConfig REQUIRED)
+find_package(PNG REQUIRED)
 
 find_package(Boost REQUIRED COMPONENTS system filesystem date_time locale)
 
@@ -22,7 +23,8 @@ add_subdirectory(pugixml)
 add_subdirectory(nanogui-sdl)
 
 #include( ../glfw/external_zlib.cmake )
-include( ../glfw/external_libpng.cmake )
+set(png_INCLUDE_DIR ${PNG_INCLUDE_DIRS})
+set(png_STATIC_LIBRARIES ${PNG_LIBRARIES})
 
 #------------------------------------------------------------------------------
 # nlohmann/json
@@ -74,7 +76,7 @@ add_executable(yabause-retro-arena ${yabause_ra_SOURCES})
 target_link_libraries(yabause-retro-arena yabause  ${Boost_LIBRARIES} pugixml ${YABAUSE_LIBRARIES} ${PORT_LIBRARIES} nanogui stdc++fs  ${LIBCHDR_LIBRARIES} )
 target_link_libraries( yabause-retro-arena ${LIBCHDR_LIBRARIES} )
 target_link_libraries( yabause-retro-arena ${zlib_STATIC_LIBRARIES} )
-add_dependencies(yabause-retro-arena Json png zlib libchd )
+add_dependencies(yabause-retro-arena Json zlib libchdr )
 yab_port_success(yabause-retro-arena)
 set_target_properties(yabause-retro-arena PROPERTIES OUTPUT_NAME yabasanshiro)
 
